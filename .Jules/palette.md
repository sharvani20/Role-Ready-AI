# Palette's Journal - Critical UX & Accessibility Learnings

## 2025-05-18 - Form Input Accessibility with Explicit Labels
**Learning:** Form inputs without explicit `<label>` elements rely on placeholders or visual position, which reduces screen reader clarity and tap target size.
**Action:** Explicitly associate form inputs with `<label>` elements using `htmlFor` and `id` attributes.
